pipeline {
    agent any
    
    stages {
        stage('Check Ansible Version') {
            steps {
                sh 'ls -all'
                sh 'pwd'
                sh 'whoami'
            }
        }
    }
}
